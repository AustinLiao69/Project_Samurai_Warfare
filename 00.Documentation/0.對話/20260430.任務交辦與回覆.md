# 📬 20260430 — 任務交辦與回覆

---

## ✉️ AustinLiao69 → Agent｜2026-04-30

### 任務一：PM005 合併至 0000（三層架構優先）
請把 PM005 文件合併到 0000 文件，尤其是三層架構部分。

### 任務二：PM005 邏輯層缺漏模組補全
PM005 的 2.1 三層架構，邏輯層是不是有少幾個模組？請檢查 0030 資料夾內的文件，如有缺漏請更新。

### 任務三：MVP Webapp 版本
MVP 部分我想先開發一個 webapp 版，先看看樣子，如果 OK 再做 GDScript 版。
程式語言請盡量用 GDScript，如果不行的話就依照你建議（可能用 python?）

### 任務四：建立 0.對話 資料夾
用 0.對話 這個資料夾來做類似郵件的功能，把我交辦事項以及你的回覆都放在這裡，檔名範例：20260430.XXXX主題。若你有任何想法、或是需要更多資訊的話，就可以回在該檔案裡，就像是 email 一樣。

---

## ✉️ Agent → AustinLiao69｜2026-04-30（第一封）

### 任務一 ✅ 完成：PM005 合併至 0000

已將 PM005 的全部技術架構內容整合進 `0000.Product_SPEC.md` 的第 2 章，包含：
- **技術選型表**（引擎/語言/版本控制/存檔格式/字型）
- **三層架構圖**（展示層 / 邏輯層 / 資料層）
- **核心設計原則**（資料/邏輯分離、Signal 驅動 UI、Command Pattern）
- **Godot 場景樹**（完整 Node 結構）
- **更新版資料結構**（四面向屬性已統一為 military/strategy/politics/charisma）
- **Command Pattern 與 GameEvent 結構**
- **資料流圖**（玩家命令流程 + 回合結算流程）
- **存檔系統設計**（JSON 格式 + Migration 機制）
- **GDScript 命名規範與檔案目錄結構**
- **效能最佳化指引**

PM005 原始文件保留，標注「已合併至 0000 v2.0」，作為版本歷史存查。

---

### 任務二 ✅ 完成：補充三個缺漏模組

對照 0030 資料夾的 12 份文件，PM005 原始三層架構邏輯層共**缺少 3 個模組**：

| 缺漏模組 | 對應文件 |
|---------|---------|
| PostWarModule（戰後處置） | 0009.戰後處置模組.md |
| FactionModule（派系思想） | 0010.派系思想模組.md |
| RetainerEvalModule（武將評價） | 0011.武將評價模組.md |

已在 0000 的三層架構圖和 Godot 場景樹中補全這 3 個模組。

---

### 任務三 ✅ 進行中：MVP Webapp

**語言選擇理由：**
GDScript 是 Godot 的專用語言，目前無法直接在瀏覽器中執行（Godot HTML5 匯出需要完整遊戲工程，MVP 原型階段過重）。因此採用 **Python + Flask** 作為 webapp 原型，後續確認視覺方向後，以 GDScript / Godot 正式重做。

**技術選型：**
- 後端：Python 3 + Flask
- 前端：Jinja2 模板 + CSS（仿古風戰情室樣式）
- 資料：以 PM008 MVP 初始資料為 mock data
- 無需資料庫：純 Python dict 模擬 GameData

**已建立的 webapp 頁面：**
| 頁面 | 路徑 | 說明 |
|------|------|------|
| 主地圖 | `/` | 四城地圖 + 兩勢力 HUD + 回合資訊 |
| 家臣列表 | `/retainers` | 八位初始武將、階級、忠誠度分級 |
| 政務選單 | `/politics` | 九項政務選單（第二層子選單） |
| 評議室 | `/council` | 評議流程示意（家臣表態） |

webapp 啟動指令：`python3 webapp/app.py`
預覽網址：`http://localhost:5000`

---

### 任務四 ✅ 完成：0.對話 資料夾建立

資料夾已建立於 `00.Documentation/0.對話/`，本檔案即為第一封往來信件。
**命名規則**：`YYYYMMDD.主題簡述.md`
**使用方式**：
- 您交辦任務 → 寫在上方 AustinLiao69 區塊
- 我回覆/提問 → 寫在 Agent 區塊
- 同一天多封信 → 檔名加後綴，如 `20260430.任務交辦與回覆_2.md`

---

### 待確認事項（請回覆）

1. **Webapp 視覺方向**：目前採用「暗色戰情室 + 古風書法字體」風格，是否符合預期？或有偏好參考的視覺參考（如《信長之野望》、《十字軍之王》、其他）？
   =>此階段先不用

3. **Webapp 互動深度**：這次 MVP webapp 是純靜態的 UI 預覽，還是希望有基本互動（如點擊政務選單後有回應、忠誠度可以變動）？
   =>要有基本互動，而且必須要遵照PM006.MVP_SRS.md的開發方向

5. **PM005 處置**：PM005 原文已合併至 0000，可否將 PM005 保留作為版本歷史？或是直接刪除？
   =>請直接刪除
6. Austin：請把所有MVP相關文件及代碼都放到0050.MVP資料夾，代碼相關路徑也請依此重新設置

---

*Agent*
*2026-04-30*

---

## ✉️ Agent → AustinLiao69｜2026-04-30（第二封）

收到，已完成全部四項指示：

### A ✅ PM005 刪除
`PM005.技術架構說明.md` 已直接刪除（不保留）。

---

### B ✅ MVP 代碼搬遷至 0050.MVP

全部 MVP 代碼已遷移至 `00.Documentation/0050.MVP/webapp/`，目錄結構如下：

```
00.Documentation/0050.MVP/
├── PM006.MVP_SRS.md
├── PM007.MVP劇本_永祿三年.md
├── PM008.MVP初始資料.md
└── webapp/
    ├── app.py               ← Flask 主程式（路由 + API）
    ├── data/
    │   └── initial.py       ← PM008 初始資料（含升級規格表）
    ├── game/
    │   └── logic.py         ← 回合結算、設施升級、年貢、野戰公式
    ├── static/
    │   ├── css/style.css    ← 戰情室樣式
    │   └── js/game.js       ← AJAX 互動邏輯
    └── templates/
        ├── base.html        ← 共用導航 + Modal + Toast
        ├── index.html       ← 天下地圖（郡點可點擊）
        ├── retainers.html   ← 家臣一覽
        ├── politics.html    ← 政務選單
        └── council.html     ← 評定會議
```

舊 `webapp/` 目錄已刪除。Workflow 啟動指令已更新為：
`python3 '00.Documentation/0050.MVP/webapp/app.py'`

---

### C ✅ 基本互動（遵照 PM006）

依 PM006 UC 實作的互動功能：

| Use Case | 互動功能 |
|---------|---------|
| UC-004 結束回合 | 「結束回合」按鈕 → 後端資源結算 → 月份推進 → 顯示日誌 |
| UC-010 查看郡詳情 | 點擊地圖郡點 → 彈出 Modal 面板（設施/年貢/產出/家臣） |
| UC-011 建設升級設施 | Modal 內「發出建設命令」→ 扣金/設施進入建設狀態 |
| UC-012 調整年貢率 | Modal 內拉桿（10%～60%）→ 即時預估產出 → 送出保存 |
| UC-022 攻郡野戰 | 點擊敵方郡 → 選出兵武將 → 公式結算 → 顯示戰報 |

資源公式、損兵公式均遵照 PM006 §4.2.2 / §4.3.3。
年貢 > 30% 時，每月自動依公式 `-(rate-30)×0.5` 扣忠誠度（§4.2.4）。

---

### 備註

- 地圖上的郡以小圓點標示（紅=織田、藍=今川），點擊即開啟詳情
- 有「重置遊戲」按鈕可回到 PM008 初始狀態
- 評定會議頁面的「延議」按鈕會直接觸發回合結算

---

*Agent*
*2026-04-30*
